[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["cd backend && npm install --production"]

[phases.build]
cmds = ["cd backend && npm run verify"]

[start]
cmd = "cd backend && npm start" 